<div class="col s12">
  <%= form_with(url: '/search', local: true ) do |f| %>
    <div class="row">
      <div class="input-field col s9">
        <%= f.text_field :title, class: 'field' %>
        <%= f.label :title, "Buscar por título do projeto" %>
      </div>

      <div class="input-field col s3">
        <button class="btn btn-small custom-grey darken-1">
          <i class="material-icons small right">search</i>
          Buscar
        </button>
      </div>
    </div>
  <% end %>
</div>
